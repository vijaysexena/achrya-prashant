import { DecimalToTimePipe } from './decimal-to-time.pipe';

describe('DecimalToTimePipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalToTimePipe();
    expect(pipe).toBeTruthy();
  });
});
